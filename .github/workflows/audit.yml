name: Security Audit

on:
  push:
    paths: 
      - '**'
  pull_request:
    paths: 
      - '**'
  schedule:
    - cron: '0 0 * * 1'

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Run security-audit
        run: echo "security check here"
